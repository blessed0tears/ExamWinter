<Page x:Class="FragrantWorld.Pages.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CartPage"
      Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ИСПРАВЛЕНО: Padding заменен на Margin -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Background="#FFCC99" Margin="10">
            <Button x:Name="BackButton" Content="Назад" Width="100" Click="BackButton_Click" Background="#CC6636"/>
            <TextBlock Text="Корзина" FontSize="16" FontWeight="Bold" Margin="20,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" x:Name="CartScrollViewer">
            <StackPanel x:Name="CartItemsPanel"/>
        </ScrollViewer>

        <!-- ИСПРАВЛЕНО: Padding заменен на Margin -->
        <StackPanel Grid.Row="2" Background="#FFEBCD" Margin="10">
            <TextBlock x:Name="TotalTextBlock" FontSize="14" FontWeight="Bold" Margin="5"/>
            <Button x:Name="CheckoutButton" Content="Оформить заказ" Click="CheckoutButton_Click" 
                    Background="#CC6636" Foreground="White" FontSize="14" Padding="10" Margin="5"/>
        </StackPanel>
    </Grid>
</Page>