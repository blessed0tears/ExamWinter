<Page x:Class="FragrantWorld.Pages.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CartPage"
      Loaded="Page_Loaded"
      Background="#FFF0F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Background="#FFB6C1" Margin="5">
            <Button x:Name="BackButton" Content="Назад" Width="100" Click="BackButton_Click" Background="#FF69B4" Foreground="White"/>
            <TextBlock Text="Корзина" FontSize="16" FontWeight="Bold" Margin="20,0,0,0" VerticalAlignment="Center" Foreground="#C71585"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" x:Name="CartScrollViewer" Background="#FFF0F5">
            <StackPanel x:Name="CartItemsPanel"/>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Background="#FFE4E9" Margin="5">
            <TextBlock x:Name="TotalTextBlock" FontSize="14" FontWeight="Bold" Margin="5" Foreground="#C71585"/>
            <Button x:Name="CheckoutButton" Content="Оформить заказ" Click="CheckoutButton_Click" 
                    Background="#FF69B4" Foreground="White" FontSize="14" Margin="5"/>
        </StackPanel>
    </Grid>
</Page>